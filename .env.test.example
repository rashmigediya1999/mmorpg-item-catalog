# Test environment variables
NODE_ENV=test
PORT=3001

# Test database (for local testing)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=game_catalog_test
# DB_USER=postgres
# DB_PASSWORD=postgres

# Test database (for Docker testing)
# These values will be overridden by the values in docker-compose.test.yml
DB_HOST=postgres-test
DB_PORT=5432
DB_NAME=game_catalog_test
DB_USER=postgres
DB_PASSWORD=postgres

# Test JWT secret
JWT_SECRET=test-secret

# Log level for tests (error, warn, info, http, debug)
LOG_LEVEL=error